import AdminSettings from "@/sections/admin/settings/AdminSettings";

export const metadata = {
  title: "Settings — Admin",
};

export default function SettingsPage() {
  return <AdminSettings />;
}
